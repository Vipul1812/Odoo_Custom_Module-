
    <!-- ============================= -->
    <!-- Custom Header for PDF Layout -->
    <!-- ============================= -->
    <!-- <template id="estate_property_report_template"
              inherit_id="web.external_layout_standard">

        <!-- Replace HEADER (first div) -->
        <xpath expr="div[1]" position="replace">

            <t t-set="company" t-value="company or env.company"/>

            <div t-attf-class="header o_company_#{company.id}_layout"
                 t-att-style="report_header_style">

                <div class="row" style="font-family:'Trebuchet MS', sans-serif;">

                    <!-- Company Address -->
                    <div class="col-5 mb4">
                        <div t-field="company.partner_id"
                             t-options='{
                                 "widget": "contact",
                                 "fields": ["name", "address"],
                                 "no_marker": true
                             }'/>
                    </div>

                    <!-- Logo -->
                    <div class="col-2 mb4 text-center">
                        <img t-if="company.logo"
                             t-att-src="image_data_uri(company.logo)"
                             style="max-height:80px;"
                             alt="Logo"/>
                    </div>

                    <!-- Page + Print Date -->
                    <div class="col-5 mb4 text-end">
                        <t t-if="report_type == 'pdf'">
                            Page <span class="page"/> / <span class="topage"/>
                        </t>
                        <div>
                            Print Date:
                            <span t-esc="datetime.datetime.now().strftime('%Y-%m-%d %H:%M:%S')"/>
                        </div>
                    </div>

                </div>

                <div class="row">
                    <div class="col-12">
                        <div style="border-bottom:1px solid black;"/>
                    </div>
                </div>

            </div>

        </xpath>

    </template> -->

    <!-- ================= -->
    <!-- Report Action -->
    <!-- ================= -->

</odoo>
